/*
 The app's settings file
 Usage:

    var config = require('config');
    var dbSettings = config.get('cv-builder.db');

    // access key to get your value

    var host = dbSettings['host']
    ...
*/

{
  "cv-builder": {
    "db": {
      "connectionPool": 10,
      "host": "localhost",
      "dbName": "CVBuilder",
      "user": "root",
      "pass": "root"
    },
    "email": {
      "from": "no-reply@example.com",
      "host": "smtp.gmail.com",
      "port": 465,
      "secureConnection": true,
      "transportMethod": "SMTP",
      "auth": {
        "user": "tungchelsea21@gmail.com",
        "pass": "chelsea0908953800"
      }
    }
  }
}